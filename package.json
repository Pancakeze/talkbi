{
  "name": "talkbi",
  "version": "0.1.0",
  "private": true,
  "description": "TalkBI monorepo — apps/web (Vite + React), apps/api (FastAPI), infra (Docker)",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev -w talkbi-web",
    "build:web": "npm run build -w talkbi-web",
    "test:web": "npm run test -w talkbi-web"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
